<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PGR2081_Exam" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Remove action bar-->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:navigationBarColor">@color/secondaryblack</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/mainred</item>
        <item name="colorPrimaryVariant">@color/secondaryblack</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/mainred</item>
        <item name="colorSecondaryVariant">@color/green</item>
        <item name="colorOnSecondary">@color/secondarygray</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PGR2081_Exam.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PGR2081_Exam.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>